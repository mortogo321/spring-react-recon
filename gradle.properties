org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configuration-cache=true
org.gradle.jvmargs=-Xmx2g -XX:+UseParallelGC -Dfile.encoding=UTF-8

# Dependency overrides applied on top of the Spring Boot BOM (see gradle/libs.versions.toml).
# We deliberately run ahead of Boot's managed versions for Flyway and the MySQL driver.
