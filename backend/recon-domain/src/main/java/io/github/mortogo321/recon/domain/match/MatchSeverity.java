package io.github.mortogo321.recon.domain.match;

/** Drives triage order in the ops console and the alerting thresholds on a run. */
public enum MatchSeverity {
    INFO,
    WARNING,
    CRITICAL
}
